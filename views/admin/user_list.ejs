<%- include('../layout/start') %>
<section class=" m-0 row vw-100" style="background-color: rgba(5, 5, 5, 0.938);" >
  <%- include('../layout/adminNavbar') %>

  <!-- table -->
  <section class= "col-12 col-md-10 scroll   vh-100 py-3">
    <div class="p-2 radius row bg-dark ">
  <table style=" color: aliceblue; width: 80%;" class="table table-dark col-12 ">
    <thead>
       
      <tr>
        <th scope="col">ID</th>
        <th scope="col">NAME</th>
        <th scope="col">EMAIL</th>
        <th scope="col">VERIFY</th>
        <th scope="col">MOBILE</th>
        <th scope="col">
            USE MANGEMEND
        </th>

      </tr>
    </thead>
    <tbody>
      <tr>
        <% for(let i=0;i<userData.length;i++){ %>
        <th scope="row"><%= i+1 %></th>
        <td><%= userData[i].name %></td>
        <td><%= userData[i].email %></td>
        <% if(userData[i].is_verfyed==true){ %>
        <td>verifyed</td>
        <% }else{ %>
            <td>not verifyed</td>
            <% } %>
        <td><%= userData[i].mobile %></td>
        <td>
            <% if(userData[i].is_blocked==false){ %>
        <a href="/admin/user-block?id=<%=userData[i]._id %>"><button class="btn btn-primary">BLOCK</button></a>
        <% }else{ %>
        <a href="/admin/user-block?id=<%=userData[i]._id %>"><button class="btn btn-primary">UNBLOCK</button></a>
        <% } %>
       
      
    </td>
      </tr>
     <% } %>
    </tbody>
  </table>
</div>
</section>
</section>
<script>
  $(document).ready(function(){
    $("#Usersearch").keyup(function(){
    let value=  $('#Usersearch').val()
    })
    $.ajax({
      url:'/search'
    })
  })
</script>

<%- include('../layout/end') %>